check to see how often app polls for new users to display + status changes

make draggable area for channels not appear in actual browser but instead have a small spacer there so the space between channel icon and left of section remain same or similar

add min and max zoom