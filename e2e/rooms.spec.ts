import { test } from "@playwright/test";

// TODO: These tests need createRoom and createRoomViaAPI helpers implemented in helpers.ts
// Skipping until those are added.
test.describe.skip("Rooms — CRUD", () => {});
test.describe.skip("Rooms — Voice", () => {});
